import { IsLocationPipe } from './is-location.pipe';

describe('IsLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
